User::sendMail now hands the template its values (it passed an empty array, so
mails went out without their code, link or name), queues a mail as JSON the
way UserJob does (the raw array failed to save, so a queued mail was never
written), and returns false and logs to the PHP error log when a send fails
instead of reporting it as sent.
