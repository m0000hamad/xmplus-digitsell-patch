Rebuilds the "disable the current subscription" toggle on the plan page. The
redesign had dropped the theme's switch markup, leaving a bare checkbox that
rendered as an empty white box with no sign of its state.
