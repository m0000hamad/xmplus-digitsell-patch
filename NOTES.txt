A working dialog for the update: an hourglass, five named stages, a progress
bar, and a result panel with the file counts. The confirmation is now part of
the same dialog instead of a browser prompt, and a proxy answering with HTML
is reported with its status code instead of a bare parse error.
