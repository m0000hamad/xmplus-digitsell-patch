Fixes a division by zero that took the dashboard down for every account with no
quota left. transfer_enable comes back from PDO as a string, so the guard's
strict === never matched "0" and the page divided by it.
